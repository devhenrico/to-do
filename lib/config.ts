/**
 * CONFIGURAÇÃO DO MODO DE FUNCIONAMENTO
 *
 * USE_OFFLINE_MODE = true  -> Usa localStorage (sem backend)
 * USE_OFFLINE_MODE = false -> Usa API do backend
 */

export const USE_OFFLINE_MODE = true;
